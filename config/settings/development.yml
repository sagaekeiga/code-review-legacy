top: http://<%= ENV['WEB_DOMAIN'] %>/
reviewers:
  sessions:
    new: http://<%= ENV['REVIEWER_DOMAIN'] %>/reviewers/sign_in
  pulls:
    show: http://<%= ENV['WEB_DOMAIN'] %>/reviewers/pulls/
links:
  home: http://<%= ENV['WEB_DOMAIN'] %>
  reviewer_repo_show: http://<%= ENV['WEB_DOMAIN'] %>/reviewers/repos/
