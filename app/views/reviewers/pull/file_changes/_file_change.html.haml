%table
  %thead
    %tr
      %th{ scope: 'col' }
  - decode_by_base64(file_change.content)&.each_line&.with_index do |line, index|
    %tbody
      %tr
        %td.index= index
        %td.file-code= coderay(line, file_change.filename)
