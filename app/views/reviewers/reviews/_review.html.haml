.panel.panel-default
  .panel-header
    .panel-title
      %h4 全体に対するコメント
    %small.text-muted
      = date_format(review.updated_at)
  .panel-body.gray-space
    .panel-text= markdown(review.body)
