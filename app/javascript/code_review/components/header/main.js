const React = require('react')
// const { pages } = require('@/util')
// const { dispatch } = require('@/store')
// const link = require('./link')
// const button = require('@/components/button')
// const logo = require('./logo.svg')
module.exports = s => {
  // const rightLinks = s.login.ok ? [
  //   link(`${i18next.t('header.setting')}`, pages.setting.path, { internalLink: true, padding: 'l' }),
  //   link(`${i18next.t('header.support')}`, 'https://support.cotobox.com/', { newTab: true }),
  //   link(`${i18next.t('header.mypage')}`, pages.mypage.path, { internalLink: true })
  // ] : [
  //     button(`${i18next.t('header.signup')}`, () => dispatch({ name: 'go', data: { path: pages.newAccount.path, query: { redirectUrl: window.location.href } } }), 14, 100, 40, { classNameName: 'ml-1' }),
  //     button(`${i18next.t('login.login')}`, () => dispatch({ name: 'go', data: { path: pages.login.path, query: { redirectUrl: window.location.href } } }), 14, 100, 40, { classNameName: 'mx-1', outline: true })
  //   ]
  // const page = Object.values(pages).find(v => v.path === s.page)
  // return (
  // )
}
